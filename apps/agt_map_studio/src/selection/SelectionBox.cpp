#include "selection/SelectionBox.h"

namespace agt_map_studio {
// SelectionBox is intentionally a small value type.  Its implementation is
// kept as a translation unit so the selection module has a stable CMake seam.
}  // namespace agt_map_studio
